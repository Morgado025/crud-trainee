<?php

$con = pg_connect("host=localhost port=5432 dbname=posvenda_estagio user=postgres password=paodebatata");

// Na criação de uma query, crie uma variável 
    // $sql_insert = "INSERT INTO tabela () values ()";

    // $res_insert = pg_query($con, $sql_insert); 
            // pg_query -  Vai executar a query (onde vai executar, o que vai executar); 
